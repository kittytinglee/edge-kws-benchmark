# Results policy

The committed JSON files contain sanitized aggregate metrics only. They must
not include usernames, server addresses, absolute paths, environment folders,
or machine identifiers.

Desktop latency is useful for development comparisons but is not a substitute
for target-board measurements. Final deployment tables should name the board,
runtime, numerical precision, thread count, warm-up policy, sample count, power
measurement method, and whether feature extraction is included.

